package org.cneko.toneko.common.mod.packets;

/**
 * Minecraft 1.20.1 registers packet identifiers directly with the receiver,
 * so there is no separate payload-type registry to initialize.
 */
public final class ToNekoPackets {
    private ToNekoPackets() {
    }

    public static void init() {
    }
}
